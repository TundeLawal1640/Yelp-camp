<% layout('layouts/Boilerplate') %>

<div class="container py-4">
  <!-- Page header -->
  <div class="d-flex align-items-baseline justify-content-between mb-3">
    <h2 class="mb-0">All Campgrounds</h2>
    <a class="btn btn-primary btn-sm" href="/campgrounds/new">Create Camp</a>
  </div>

  <% for (let campground of campgrounds) { %>
    <!-- One campground = one centered block -->
    <div class="row justify-content-center mb-4">
      <!-- Constrain width so the content sits centered (like your arrow points to) -->
      <div class="col-12 col-lg-10">
        <!-- Inner row for image + card -->
        <div class="row g-3 align-items-center">
          <!-- Image column -->
          <div class="col-12 col-md-4">
            <img
              class="img-fluid w-100 rounded"
              src="<%= campground.imageUrl %>"
              alt="<%= campground.name %> image"
              style="aspect-ratio: 4 / 3; object-fit: cover;"
            />
          </div>

          <!-- Card column -->
          <div class="col-12 col-md-8">
            <div class="card h-100">
              <div class="card-body">
                <h5 class="card-title"><%= campground.name %></h5>
                <h6 class="card-subtitle mb-2 text-body-secondary">
                  <%= campground.location %>
                </h6>
                <h6 class="card-subtitle mb-2 text-body-secondary">
                  $<%= campground.price %>
                </h6>

                <p class="card-text"><%= campground.description %></p>
                 <p><i>Posted by - <%= campground.author ? campground.author.username : "Unknown" %></i></p>


                <!-- Links -->
<a class="btn btn-primary ms-lg-2 btn-l px-3" href="/campgrounds/show/<%= campground._id %>">View camp</a>
<% if (currentUser && campground.author && campground.author.equals(currentUser._id)) { %>
  <a class="btn btn-secondary ms-lg-2 btn-l px-3"
     href="/campgrounds/edit/<%= campground._id %>">Edit camp</a>
<% } %>


              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  <% } %>
</div>